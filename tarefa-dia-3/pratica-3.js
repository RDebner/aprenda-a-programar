let i = 1 
let a = 50
 while (i<=a){
    if (i % 2 == 0){
        let par = `${i} é par`
        console.log(par)
    } else {
        let impar = `${i} é impar`
        console.log(impar)
    }
    i++
 }
